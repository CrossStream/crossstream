#!/bin/echo docker build . -f
# -*- coding: utf-8 -*-
# SPDX-License-Identifier: Apache-2.0

FROM debian:bookworm

# RUN echo "# log: Setup system" #   && set -x #   && apt-get update -y #   && apt-get install -y sudo  #   && date -u

# RUN echo "# log: Install ${project}" #   && set -x #   && date -u
